# 本番環境では必ずこのファイルをコピーして .env を作成してください
# cp .env.example .env

# フロントエンドのURL (カンマ区切りで複数指定可)
# JSON配列形式で記述してください
ALLOWED_ORIGINS=["http://localhost:3000"]

# 環境 (development / production)
ENVIRONMENT=development
